# Windows SEO browser-smoke evidence

Sanitized PASS report and the PowerShell 5.1 launcher diff.
Does not include fixture SQLite, dist, node_modules, cookies, or a browser profile.

Windows host name is retained as identity of the smoke machine.
Personal filesystem prefixes were replaced with `<test-root>`.
