# Яндекс Метрика — безопасное подключение (подготовка)

Счётчик: `112814607`. Документ не заявляет юридическую готовность и не разрешает production ON.

## Build-time флаг

`VITE_YANDEX_METRIKA_ENABLED` вшивается в JS на `vite build`.

- Значение в `server/.env` или в shell **после** сборки не переписывает уже отданный бандл.
- Чтобы выключить на сервере: собрать UI **без** `=1`, выложить новые hashed assets, затем пользователи должны перезагрузить вкладку.
- Уже открытые вкладки со старым бандлом продолжают старый код до reload.
- Отзыв согласия в новой версии останавливает дальнейший сбор **в этой вкладке** сразу (`destruct` + `disableYaCounter` + снятие `tag.js`).
- `VITE_YANDEX_METRIKA_TEST_MODE=1` только для isolated off-live проверок. В этом режиме localhost разрешён, а `mc.yandex.ru` не подключается.

## Рабочий порядок отключения

1. Не ставить `VITE_YANDEX_METRIKA_ENABLED=1` в production-сборке (текущий `.env.production` так и живёт).
2. Если флаг уже был влит в live JS: новая сборка без флага + выкладка assets + reload вкладок.
3. Пользователь может отозвать согласие в футере «Настройки аналитики» — это останавливает дальнейший сбор, но не удаляет уже отправленное.
4. Откат ветки / не деплоить её.

## Заказы и реклама

- `order_submitted` только после успеха публичного `POST /api/public/orders`.
- Заказы ЛК (`PUT /api/state/orders`) **вне аналитики**. Рекламная отчётность по кабинету и по «оплаченный новый клиент» из Метрики **не считается**.
- `order_submitted` ≠ оплата и ≠ новый покупатель. Стоимость оплаченного нового клиента — только сверка с фактическими продажами.
- В URL hit сохраняются только безопасные по **ключу и значению** параметры: `utm_*` (токены `[A-Za-z0-9._~-]{1,80}`), `yclid` / `ymclid` / `ysclid` / `gclid` (1–128 `[A-Za-z0-9]`).
- `utm_*` с пробелами, кириллицей, email или телефоном (включая `921…` / `7921…` без `+`) отбрасывается — имя параметра само по себе не доказывает отсутствие ПДн.
- Длинные числовые `yclid` сохраняются: правило click id не ищет телефонную подстроку внутри `[A-Za-z0-9]{1,128}`. Этот формат не доказывает отсутствие любых персональных данных.

## Согласие

Отдельный opt-in, не связан с обязательным согласием при заказе и со страницей `/personal-data-consent`.
Запись: `localStorage.clover-analytics-consent-v1` = `{"version":1,"status":"granted"|"denied"}`.
Нет записи / другая version = нет решения ≠ согласие.

## Политика и согласие (тексты внесены в код, не юр. заключение)

Русские тексты `/privacy-policy` и `/personal-data-consent` обновлены в `infoPageContent.js`.
Фраза «использование сайта означает согласие» убрана. Заказ отделён от рекламы и аналитики.
Баннер и «Настройки аналитики» показываются только если `VITE_YANDEX_METRIKA_ENABLED=1`.
Ссылка на `/personal-data-consent` стоит у оформления заказа; чекбокса аналитики/рекламы нет.

Документ по-прежнему **не** является юридическим заключением и **не** включает Метрику.

Размещение сайта и рабочей базы: VPS ООО «Филанко Санкт-Петербург» (ИНН 7838492138, КПП 783801001), страна — Россия. Источник — реквизиты поставщика и подтверждение владельца. Это **не** независимая проверка дата-центра и **не** распространяется на почту, Telegram, сервисы ООО «ЯНДЕКС» и неподтверждённые внешние копии. Эти оговорки аудита в публичную политику не выносятся.

Подп. 8 п. 1 ст. 23 НК РФ (КонсультантПлюс, ред. от 04.08.2026; изм. от 17.02.2021 № 6-ФЗ): сохранность данных учёта и документов, нужных для исчисления и уплаты налогов, — **пять лет**, не четыре. Публичный текст разделяет учётные документы и остальные данные заказа / профиля / обращений. Пять лет — минимальная сохранность учётных документов, не дата обязательного удаления.

Техническое поведение хранения (не срок для посетителя):

- Автоудаления рабочих заказов, обращений, учёток кабинета и журнала действий в рабочей системе нет.
- Ротация архивов на том же сервере: пакеты `daily-backup` старше 14 дней (`CLOVER_BACKUP_KEEP_DAYS`, по умолчанию 14); автоматические полные копии старше 30 дней или свыше 50 файлов. Это замена технических архивов, не срок хранения заказа.
- CLI/таймер audit retention в репозитории есть и **не** включены.
- База и очистка в этом цикле не меняются.
- Выключение флага Метрики или отзыв согласия не удаляют уже отправленные данные у ООО «ЯНДЕКС».

Пробелы для владельца (не выдумывать в политике):

- Юрлицо почтового сервиса `9112368277@mail.ru` и страна/место хранения писем не подтверждены.
- Юрлицо Telegram и страна хранения уведомлений не подтверждены.
- Отдельное внешнее хранилище резервных копий не подтверждено.
- Срок хранения внутри 1С не задан.
- Для журнала действий отдельного числового срока в законе нет.
- Специальные сроки (убытки, страховые взносы и т.п.) в политике не перечисляются.

Windows `LEGAL_PAGES_BROWSER_PASS` относился только к dirty candidate в `yandex-metrika-safe-init` и **не** переносится на эту ветку автоматически.

## Состав релиза

Ветка основана на `99e45c4` (PR #141) и несёт интеграцию PR #142 (`23414ee`). Деплой `main` не равен «поставить только Метрику»: перед установкой сверить полный состав релиза и получить отдельное разрешение на включение.
