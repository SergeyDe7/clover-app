CLOVER — ИСПРАВЛЕНИЕ HTTP 404 / ОЧЕРЕДИ 1С TEST V3

Исправляет две подтверждённые причины:
1. Старая кнопка «Отправить» помечала заказ как отправленный симулятором вместо постановки в реальную очередь.
2. В backend использовалось несуществующее имя normalizeOrderExchange, из-за чего могла возникать внутренняя ошибка при обновлении заказов.

Пакет:
- не меняет server\.env;
- не меняет server\uploads;
- не создаёт новый заказ;
- не вызывает endpoint получения заказа;
- работает только с заказом CL-260728-152400-536 на 210 руб.;
- не допускает возврат в очередь заказа, реально подтверждённого 1С;
- создаёт резервную копию server.js и базы перед изменениями;
- при ошибке выполняет автоматический откат;
- после запуска проверяет через безопасный queue-status, что заказ первый в очереди 1С TEST.

ЗАПУСК
1. Распакуйте архив.
2. Запустите INSTALL_ONEC_QUEUE_FIX_TEST.bat.
3. После сообщения «ГОТОВО» один раз получите заказ в 1С TEST.

Рабочую базу VLAVKA не открывать и не изменять.
Повторно кнопки очереди в Clover не нажимать.
