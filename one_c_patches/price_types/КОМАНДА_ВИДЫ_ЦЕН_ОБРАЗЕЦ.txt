&НаКлиенте
Процедура ОбработкаКоманды(ПараметрКоманды, ПараметрыВыполненияКоманды)
	
	// Вызывать с клиента — иначе безопасный режим блокирует HTTP.
	
	Результат = Clover_ОбменКлиент.ОтправитьВидыЦенВClover();
	
	Если Результат.КодСостояния = 200 Тогда
		Сообщить("Виды цен переданы в Clover: " + Строка(Результат.Количество));
	ИначеЕсли Результат.КодСостояния = -1 Тогда
		Сообщить("Ошибка связи / ключа: " + Строка(Результат.Ошибка));
	Иначе
		Сообщить("Clover код " + Строка(Результат.КодСостояния) + " / " + Строка(Результат.Ошибка));
	КонецЕсли;
	
КонецПроцедуры
