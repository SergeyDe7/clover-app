&НаКлиенте
Процедура ОбработкаКоманды(ПараметрКоманды, ПараметрыВыполненияКоманды)

	// Важно: вызывать с клиента, НЕ через &НаСервере —
	// иначе 1С включает безопасный режим и блокирует HTTPСоединение.

	// Было (один заказ за клик):
	// Результат = Clover_ОбменКлиент.СоздатьТестовыйЗаказ();

	Результат = Clover_ОбменКлиент.СоздатьВсеТестовыеЗаказы();

	Если Результат.Успех Тогда
		Текст = "Принято заказов из Clover: " + Строка(Результат.Количество);
		Если СокрЛП(Строка(Результат.Номера)) <> "" Тогда
			Текст = Текст + " № " + Строка(Результат.Номера);
		КонецЕсли;
		Если СокрЛП(Строка(Результат.Ошибка)) <> "" Тогда
			Текст = Текст + ". " + Строка(Результат.Ошибка);
		КонецЕсли;
		Сообщить(Текст);
	ИначеЕсли СокрЛП(Строка(Результат.Ошибка)) = "Нет новых заказов" Тогда
		Сообщить("Нет новых заказов");
	Иначе
		Сообщить("Ошибка приёма заказов Clover: " + Строка(Результат.Ошибка));
	КонецЕсли;

КонецПроцедуры
